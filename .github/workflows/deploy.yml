name: Deploy
description: Deploy the application when a new version is tagged.

on:
  push:
    tags:
      - v*

jobs:
  Build-check-and-deploy:
    permissions:
      contents: read
    uses: tylerkelly13/.github/.github/workflows/node-check-an-deploy.yml@main
    with:
      node-version: "24.x"
      pnpm-version: "latest"
